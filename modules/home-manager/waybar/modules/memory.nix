{
  programs.waybar.settings.mainBar = {
    "memory" = {
      interval = 10;
      format = "{icon}   {used:0.1f}G";
      format-icons = "";
      tooltip-format = "{percentage}% used";
    };
  };
}
